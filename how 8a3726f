[33mcommit 8a3726f40a594d91a7c08540733773e8c2793e87[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mtest-from-eabae366[m[33m)[m
Author: Mario Espinoza <mcm5owsla@gmail.com>
Date:   Sat Oct 4 18:45:59 2025 -0700

    chore: unify console 'i' for USB/BT; adjust acoustic level curve; add ring-down groundwork for DECAY

[1mdiff --git a/a04fb21-from-eabae.patch b/a04fb21-from-eabae.patch[m
[1mnew file mode 100644[m
[1mindex 0000000..76ff0fa[m
Binary files /dev/null and b/a04fb21-from-eabae.patch differ
[1mdiff --git a/lib/controllers/AcousticInjector.cpp b/lib/controllers/AcousticInjector.cpp[m
[1mindex b380911..28acccc 100644[m
[1m--- a/lib/controllers/AcousticInjector.cpp[m
[1m+++ b/lib/controllers/AcousticInjector.cpp[m
[36m@@ -4,6 +4,8 @@[m
 #include "StateMachine.h"[m
 [m
 AcousticInjector* AcousticInjector::_instance = nullptr;[m
[32m+[m[32mextern StateMachine* fsm;[m
[32m+[m
 [m
 // Nuevo:[m
 uint8_t AcousticInjector::_sineTable[AcousticInjector::TABLE_SIZE] = {[m
[36m@@ -64,7 +66,7 @@[m [mvoid AcousticInjector::start(float level) {[m
   _levelInt    = uint8_t(_level * 255);[m
 [m
   // 2) Configurar “punto de partida” y “objetivo”[m
[31m-  const float startFreq  = 2000.0f;               // arranque en 2 kHz[m
[32m+[m[32m  const float startFreq  = 2000.0f;            // arranque en 2 kHz[m
   float       targetFreq = _targetFrequency;      // debe venir de mapLoadToWaveFrequency()[m
 [m
   _currentFrequency = startFreq;[m
[36m@@ -79,9 +81,6 @@[m [mvoid AcousticInjector::start(float level) {[m
   timerAlarmEnable(_timer);[m
 }[m
 [m
[31m-[m
[31m-[m
[31m-[m
 void AcousticInjector::stop() {[m
   resetInternal();[m
   _active = false;[m
[36m@@ -92,8 +91,7 @@[m [mvoid AcousticInjector::stop() {[m
   _levelInt = 0;[m
 }[m
 [m
[31m-void AcousticInjector::setLevel(float level) {[m
[31m-  [m
[32m+[m[32mvoid AcousticInjector::setLevel(float level) {[m[41m  [m
   _targetLevel = constrain(level, 0.0f, 1.0f);[m
 }[m
 [m
[36m@@ -117,9 +115,6 @@[m [mvoid AcousticInjector::update() {[m
   _levelInt = uint8_t(_level * 255);[m
 }[m
 [m
[31m-[m
[31m-[m
[31m-[m
 void IRAM_ATTR AcousticInjector::onTimer() {[m
   if (!_instance) return;[m
 [m
[36m@@ -224,11 +219,17 @@[m [mvoid AcousticInjector::testSimple() {[m
 [m
 float AcousticInjector::mapLoadToWaveFrequency(float percent) {[m
     percent = constrain(percent, 0.0f, 100.0f);[m
[32m+[m[32m    // Normalizado 0..1[m
[32m+[m[32m    float x = percent / 100.0f;[m
 [m
[31m-    float logMin = logf(_freqMin);[m
[31m-    float logMax = logf(_freqMax);[m
[31m-    float logFreq = logMin + (percent / 100.0f) * (logMax - logMin);[m
[32m+[m[32m    // Controla la curvatura: >1 = lento al inicio, rápido al final[m
[32m+[m[32m    const float easingExponent = 2.2f; // prueba 1.8 .. 3.0[m
[32m+[m[32m    float eased = powf(x, easingExponent);[m
 [m
[32m+[m[32m    // Mapear en escala logaritmica para respuesta perceptual natural[m
[32m+[m[32m    float logMin = logf(_freqMin);   // _freqMin = 2000.0f[m
[32m+[m[32m    float logMax = logf(_freqMax);   // _freqMax = 6500.0f[m
[32m+[m[32m    float logFreq = logMin + eased * (logMax - logMin);[m
     return expf(logFreq);[m
 }[m
 [m
[36m@@ -249,9 +250,6 @@[m [mvoid AcousticInjector::updateWaveFrequency(float freqHz) {[m
     timerAlarmWrite(_timer, uint32_t(periodUs), true);[m
 }[m
 [m
[31m-[m
[31m-[m
[31m-[m
 void AcousticInjector::setFrequencyRangeOption(FrequencyRangeOption option) {[m
     _freqOption = option;[m
 [m
[36m@@ -269,11 +267,11 @@[m [mvoid AcousticInjector::setFrequencyRangeOption(FrequencyRangeOption option) {[m
             _freqMax = 6500.0f;[m
             break;[m
         case RANGE_4:[m
[31m-            _freqMin = 4400.0f;[m
[32m+[m[32m            _freqMin = 2000.0f;[m
             _freqMax = 6500.0f;[m
             break;            [m
         default:[m
[31m-            _freqMin = 5800.0f;[m
[32m+[m[32m            _freqMin = 2000.0f;[m
             _freqMax = 6500.0f;[m
             break;[m
     }[m
[1mdiff --git a/lib/controllers/AcousticInjector.h b/lib/controllers/AcousticInjector.h[m
[1mindex 1b0921c..575b9d3 100644[m
[1m--- a/lib/controllers/AcousticInjector.h[m
[1m+++ b/lib/controllers/AcousticInjector.h[m
[36m@@ -44,7 +44,9 @@[m [mpublic:[m
       _decayStartMillis = nowMillis;[m
       _lastFrequency = _currentFrequency; // guarda la frecuencia actual[m
   }[m
[31m-[m
[32m+[m[32m  float getDecayStartFrequency() {[m
[32m+[m[32m    return _lastFrequency;[m
[32m+[m[32m  }[m
 [m
 [m
   static AcousticInjector* _instance;[m
[36m@@ -70,7 +72,7 @@[m [mprivate:[m
   static constexpr float FREQ_RAMP_STEP = 20.0f; // Hz por llamada a update()[m
   static constexpr float DEFAULT_SAMPLE_RATE = 32000.0f; // tasa de muestreo segura[m
   FrequencyRangeOption _freqOption = RANGE_3;[m
[31m-  float _freqMin = 5500.0f;[m
[32m+[m[32m  float _freqMin = 2000.0f;[m
   float _freqMax = 6500.0f;[m
   bool _skipSmoothStep = false;[m
   void resetInternal() {[m
[1mdiff --git a/lib/core/StateMachine.cpp b/lib/core/StateMachine.cpp[m
[1mindex 57311df..64c4afe 100644[m
[1m--- a/lib/core/StateMachine.cpp[m
[1m+++ b/lib/core/StateMachine.cpp[m
[36m@@ -27,7 +27,7 @@[m [mvoid StateMachine::begin(bool hasCalibration,[m
     vortexPending       = false;[m
     vortexStartMillis   = 0;[m
     decayStartMillis = 0;[m
[31m-    lastTPSPercent      = 0.0f;[m
[32m+[m[32m    lastAcousticLevel      = 0.0f;[m
     lastMAPPercent      = 0.0f;[m
 [m
     if (actuators) {[m
[36m@@ -40,10 +40,6 @@[m [mvoid StateMachine::begin(bool hasCalibration,[m
     Serial.println(static_cast<int>(current));[m
 }[m
 [m
[31m-SystemState StateMachine::getState() const {[m
[31m-    return current;[m
[31m-}[m
[31m-[m
 float StateMachine::getLevel() const {[m
     return tpsNormalized;[m
 }[m
[36m@@ -162,7 +158,7 @@[m [mvoid StateMachine::update(float mapLoadPercent,[m
 [m
         case SystemState::VORTEX:[m
             if (_mapLoadPercent < thresholds.VORTEX_MAP_OFF[m
[31m-             || _tpsLoadPercent < thresholds.VORTEX_TPS_OFF) {[m
[32m+[m[32m             && _tpsLoadPercent < thresholds.VORTEX_TPS_OFF) {[m
                 unsigned long now = millis();   [m
                 current = SystemState::DECAY;[m
                 vortexPending       = false;[m
[36m@@ -218,52 +214,81 @@[m [mvoid StateMachine::handleActions() {[m
     if (!sensors || !actuators || !calibMgr) return;[m
 [m
     if (current == SystemState::BEAM[m
[31m-     || current == SystemState::VORTEX) {[m
[31m-        [m
[32m+[m[32m    || current == SystemState::VORTEX) {[m
[32m+[m
[32m+[m[32m        // Lecturas relativas[m
         float deltaTPSPercent = sensors->getRelativeTPSLoad(tpsInitialPercent);[m
         float deltaMAPercent  = sensors->getRelativeMAPLoad(mapInitialPercent);[m
 [m
[31m-        if (abs(deltaTPSPercent - lastTPSPercent) > 0.1f[m
[31m-         || abs(deltaMAPercent  - lastMAPPercent ) > 0.1f) {[m
[31m-            actuators->setAcousticParameters(deltaTPSPercent, deltaMAPercent);[m
[31m-            lastTPSPercent = deltaTPSPercent;[m
[31m-            lastMAPPercent = deltaMAPercent;[m
[31m-        }[m
[31m-[m
[31m-        // Normalización TPS y MAP[m
[32m+[m[32m        // Normalización TPS y MAP (0..1)[m
         float tpsRel = (sensors->readTPSLoadPercent() - tpsInitialPercent) /[m
[31m-                       (thresholds.VORTEX_TPS_ON - tpsInitialPercent);[m
[32m+[m[32m                    (thresholds.VORTEX_TPS_ON - tpsInitialPercent);[m
         float mapRel = (sensors->readMAPLoadPercent() - mapInitialPercent) /[m
[31m-                       (thresholds.VORTEX_MAP_ON - mapInitialPercent);[m
[32m+[m[32m                    (thresholds.VORTEX_MAP_ON - mapInitialPercent);[m
 [m
         tpsRel = constrain(tpsRel, 0.0f, 1.0f);[m
         mapRel = constrain(mapRel, 0.0f, 1.0f);[m
 [m
[31m-        // Cálculo de vortexLevel original[m
[32m+[m[32m        // vortexLevel basado en tpsRel * mapRel (como antes)[m
         float vortexLevel = tpsRel * mapRel;[m
[31m-        vortexLevel = (vortexLevel > 1.0f) ? 1.0f : vortexLevel;[m
[32m+[m[32m        vortexLevel = fminf(vortexLevel, 1.0f);[m
[32m+[m
[32m+[m[32m        // Curva exponencial perceptual (misma forma para ambos)[m
[32m+[m[32m        const float a = 4.0f; // curva perceptual compartida (ajustable)[m
[32m+[m[32m        float curvedVortex = (expf(a * vortexLevel) - 1.0f) / (expf(a) - 1.0f);[m
[32m+[m[32m        actuators->setVortexLevel(curvedVortex);[m
 [m
[31m-        // 🔹 Aplicar curva exponencial SOLO al level[m
[31m-        float a = 4.0f; // controla la aceleración al final[m
[31m-        float curvedLevel = (exp(a * vortexLevel) - 1.0f) / (exp(a) - 1.0f);[m
[32m+[m[32m        // acousticLevel: aplica la MISMA curva pero solo sobre tpsRel[m
[32m+[m[32m        float tpsBased = tpsRel; // escala base 0..1 usando solo TPS[m
[32m+[m[32m        float curvedAcoustic = (expf(a * tpsBased) - 1.0f) / (expf(a) - 1.0f);[m
 [m
[31m-        actuators->setVortexLevel(curvedLevel);[m
[32m+[m[32m        // setAcousticParameters espera level en 0..100 (según tu código previo)[m
[32m+[m[32m        float acousticLevel = curvedAcoustic * 100.0f;[m
[32m+[m
[32m+[m[32m        // Actualizar sólo si hay cambio significativo en level o MAP[m
[32m+[m[32m        if (fabsf(acousticLevel - lastAcousticLevel) > 0.1f[m
[32m+[m[32m        || fabsf(deltaMAPercent  - lastMAPPercent ) > 0.1f) {[m
[32m+[m[32m            actuators->setAcousticParameters(acousticLevel, deltaMAPercent);[m
[32m+[m[32m            lastAcousticLevel = acousticLevel;[m
[32m+[m[32m            lastMAPPercent = deltaMAPercent;[m
[32m+[m[32m        }[m
 [m
[31m-        // NOTA: no tocamos la frecuencia[m
[32m+[m[32m        // Mantén el update habitual[m
         actuators->update(_tpsLoadPercent, _mapLoadPercent);[m
[32m+[m[32m        return;[m
     }[m
 [m
[31m-        // ──────── DECAY ───────────────────────────────────────────────────[m
[32m+[m
[32m+[m[32m    // ──────── DECAY (ring‑down calculado en el FSM) ─────────────────────[m
     if (current == SystemState::DECAY) {[m
         // 1) Mantén la misma amplitud (vortexLevel) usando los últimos porcentajes[m
[31m-        actuators->setAcousticParameters(lastTPSPercent, lastMAPPercent);[m
[32m+[m[32m        actuators->setAcousticParameters(lastAcousticLevel, lastMAPPercent);[m
[32m+[m
[32m+[m[32m        // 2) Calcula ring-down exponencial basado en tiempo (se ejecuta cada tick)[m
[32m+[m[32m        unsigned long now = millis();[m
[32m+[m[32m        float tSec = (now - decayStartMillis) * 0.001f; // segundos[m
[32m+[m
[32m+[m[32m        // Parámetros ajustables[m
[32m+[m[32m        const float fTarget = 2000.0f;      // frecuencia de reposo[m
[32m+[m[32m        const float alpha   = 4.0f;         // amortiguamiento (mayor = más rápido)[m
[32m+[m[32m        const float finishThresholdHz = 1.0f; // criterio para acabar antes del timeout[m
 [m
[31m-        // 2) Update() avanzará la rampa de frecuencia basada en[m
[31m-        //    mapLoadToWaveFrequency() que internamente detecta DECAY[m
[31m-        actuators->update(lastTPSPercent, lastMAPPercent);[m
[32m+[m[32m        // Frecuencia de inicio: prefer getter de inicio de decay, si no usar current[m
[32m+[m[32m        float f0 = actuators->getAcousticInjector().getDecayStartFrequency() ? actuators->getAcousticInjector().getDecayStartFrequency() : actuators->getAcousticInjector().getFrequency();[m
 [m
[31m-        // 3) Al completar el tiempo de decay, apaga y vuelve a IDLE[m
[31m-        if (millis() - decayStartMillis >= decayDurationMs) {[m
[32m+[m[32m        // Modelo de ring-down: f(t) = fTarget + (f0 - fTarget) * exp(-alpha * t)[m
[32m+[m[32m        float decayFreq = fTarget + (f0 - fTarget) * expf(-alpha * tSec);[m
[32m+[m
[32m+[m[32m        // 3) Fija el objetivo de frecuencia; injector.update() hará la rampa suave[m
[32m+[m[32m        actuators->getAcousticInjector()[m
[32m+[m[32m.setTargetFrequency(decayFreq);[m
[32m+[m
[32m+[m[32m        // 4) Avanza el injector (reprograma timer con la frecuencia actual tras step)[m
[32m+[m[32m        actuators->update(lastAcousticLevel, lastMAPPercent);[m
[32m+[m
[32m+[m[32m        // 5) Condición de término: timeout o frecuencia suficientemente cercana a target[m
[32m+[m[32m        if ((now - decayStartMillis) >= decayDurationMs[m
[32m+[m[32m         || fabsf(decayFreq - fTarget) <= finishThresholdHz) {[m
             actuators->stopAcoustic();[m
             actuators->stopVortex();[m
             current = SystemState::IDLE;[m
[36m@@ -280,4 +305,3 @@[m [mvoid StateMachine::debugForceState(SystemState nuevoEstado) {[m
     }[m
 }[m
 [m
[31m-[m
[1mdiff --git a/lib/core/StateMachine.h b/lib/core/StateMachine.h[m
[1mindex e690eda..5921882 100644[m
[1m--- a/lib/core/StateMachine.h[m
[1m+++ b/lib/core/StateMachine.h[m
[36m@@ -41,10 +41,6 @@[m [mpublic:[m
    */[m
   void begin(bool hasCalibration, ActuatorManager* actuators, ThresholdManager* thresholdManagerPtr, SensorManager* sensorsPtr, CalibrationManager* calibMgrPtr);[m
 [m
[31m-  /**[m
[31m-   * Obtiene el estado actual.[m
[31m-   * @return Estado activo de la FSM.[m
[31m-   */[m
   SystemState getState() const;[m
 [m
   /**[m
[36m@@ -104,7 +100,7 @@[m [mprivate:[m
   bool vortexPending = false;  [m
   float tpsNormalized{0.0f}; [m
   float mapNormalized{0.0f}; [m
[31m-  float lastTPSPercent;   // <— último TPS%[m
[32m+[m[32m  float lastAcousticLevel;   // <— último TPS%[m
   float lastMAPPercent;   // <— último MAP%[m
   uint32_t decayStartMillis     = 0;[m
   const uint32_t decayDurationMs = 1000; // ms mínimos en COOLDOWN[m
[1mdiff --git a/lib/core/ThresholdManager.cpp b/lib/core/ThresholdManager.cpp[m
[1mindex b1f3f22..b777d6e 100644[m
[1m--- a/lib/core/ThresholdManager.cpp[m
[1m+++ b/lib/core/ThresholdManager.cpp[m
[36m@@ -5,20 +5,29 @@[m
 static constexpr const char* NVS_NAMESPACE = "thresholds";[m
 [m
 bool ThresholdManager::begin() {[m
[32m+[m[32m    Serial.println("[ThresholdManager] begin()");[m
     loadDefaults();  // Cargar llaves y valores por defecto primero[m
[32m+[m[32m    Serial.println("[ThresholdManager] Defaults loaded:");[m
[32m+[m[32m    for (const auto &p : thresholds) Serial.printf("  %s = %.3f\n", p.first.c_str(), p.second);[m
 [m
     if (!loadFromNVS()) {[m
[31m-        // Si falló la carga, mantenemos los defaults y los guardamos[m
[31m-        return saveToNVS();[m
[32m+[m[32m        Serial.println("[ThresholdManager] loadFromNVS() failed. Will save defaults to NVS.");[m
[32m+[m[32m        bool saved = saveToNVS();[m
[32m+[m[32m        Serial.printf("[ThresholdManager] saveToNVS returned %d\n", saved ? 1 : 0);[m
[32m+[m[32m        return saved;[m
     }[m
 [m
[32m+[m[32m    Serial.println("[ThresholdManager] Loaded thresholds from NVS:");[m
[32m+[m[32m    for (const auto &p : thresholds) Serial.printf("  %s = %.3f\n", p.first.c_str(), p.second);[m
     return true;[m
 }[m
 [m
 Thresholds ThresholdManager::getThresholds() const {[m
     Thresholds t;[m
[32m+[m[32m    // Añadimos debug mínimo aquí para facilitar trazado cuando se llama.[m
     t.MAP_WAKEUP_PERCENT = thresholds.at("MAP_WAKEUP_PERCENT");[m
     t.INJ_MAP_ON        = thresholds.at("INJ_MAP_ON");[m
[32m+[m[32m    t.INJ_TPS_ON        = thresholds.at("INJ_TPS_ON");[m
     t.INJ_TPS_OFF       = thresholds.at("INJ_TPS_OFF");[m
     t.INJ_MAP_OFF       = thresholds.at("INJ_MAP_OFF");[m
     t.VORTEX_TPS_ON      = thresholds.at("VORTEX_TPS_ON");[m
[36m@@ -33,16 +42,22 @@[m [mbool ThresholdManager::setThreshold(const std::string& key, float value) {[m
     auto it = thresholds.find(key);[m
     if (it != thresholds.end()) {[m
         it->second = value;[m
[32m+[m[32m        Serial.printf("[ThresholdManager] setThreshold OK: %s = %.3f\n", key.c_str(), value);[m
         return true;[m
     }[m
[32m+[m
[32m+[m[32m    Serial.printf("[ThresholdManager] setThreshold FAILED: key not found: '%s'\n", key.c_str());[m
[32m+[m[32m    for (const auto &p : thresholds) Serial.printf("  '%s'\n", p.first.c_str());[m
     return false;[m
 }[m
 [m
 bool ThresholdManager::save() {[m
[32m+[m[32m    Serial.println("[ThresholdManager] save() called");[m
     return saveToNVS();[m
 }[m
 [m
 bool ThresholdManager::reset() {[m
[32m+[m[32m    Serial.println("[ThresholdManager] reset() called");[m
     loadDefaults();[m
     return saveToNVS();[m
 }[m
[36m@@ -62,62 +77,78 @@[m [mvoid ThresholdManager::loadDefaults() {[m
     thresholds["MAP_WAKEUP_PERCENT"] = 10.0f;[m
 [m
     // Umbrales para activar la inyección acústica[m
[31m-    thresholds["INJ_TPS_ON"]  = 70.0f;   // % TPS mínimo para iniciar  ← añadido[m
[32m+[m[32m    thresholds["INJ_TPS_ON"]  = 50.0f;   // % TPS mínimo para iniciar  ← añadido[m
     thresholds["INJ_MAP_ON"]         = 50.0f;   // % MAP mínimo para iniciar[m
 [m
     // Umbrales para detener la inyección acústica[m
[31m-    thresholds["INJ_TPS_OFF"]        = 50.0f;    // % TPS para apagar[m
[32m+[m[32m    thresholds["INJ_TPS_OFF"]        = 40.0f;    // % TPS para apagar[m
     thresholds["INJ_MAP_OFF"]        = 40.0f;   // % MAP para apagar[m
 [m
     // Umbrales para activar el vortex[m
[31m-    thresholds["VORTEX_TPS_ON"]       = 75.0f;   // % TPS para activar[m
[31m-    thresholds["VORTEX_MAP_ON"]       = 70.0f;   // % MAP para activar (presión alta)[m
[32m+[m[32m    thresholds["VORTEX_TPS_ON"]       = 95.0f;   // % TPS para activar[m
[32m+[m[32m    thresholds["VORTEX_MAP_ON"]       = 95.0f;   // % MAP para activar (presión alta)[m
 [m
     // Umbral para apagar el vorte[m
[31m-    thresholds["VORTEX_TPS_OFF"]      = 60.0f;   // % TPS para apagar vortex[m
[31m-    thresholds["VORTEX_MAP_OFF"]      = 60.0f;   // % TPS para apagar vortex[m
[31m-[m
[32m+[m[32m    thresholds["VORTEX_TPS_OFF"]      = 80.0f;   // % TPS para apagar vortex[m
[32m+[m[32m    thresholds["VORTEX_MAP_OFF"]      = 80.0f;   // % TPS para apagar vortex[m
 }[m
 [m
[31m-[m
 bool ThresholdManager::loadFromNVS() {[m
[32m+[m[32m    Serial.println("[ThresholdManager] loadFromNVS() starting");[m
     Preferences prefs;[m
     if (!prefs.begin(NVS_NAMESPACE, true)) {[m
[31m-        Serial.println("ERROR: No se pudo abrir NVS para lectura");[m
[32m+[m[32m        Serial.println("[ThresholdManager] ERROR: No se pudo abrir NVS para lectura");[m
         return false;[m
     }[m
 [m
     // Si alguna clave falta, no cargamos (datos incompletos)[m
     for (const auto& key : listKeys()) {[m
[31m-        if (!prefs.isKey(key.c_str())) {[m
[32m+[m[32m        bool exists = prefs.isKey(key.c_str());[m
[32m+[m[32m        Serial.printf("[ThresholdManager] key check: %s -> %d\n", key.c_str(), exists ? 1 : 0);[m
[32m+[m[32m        if (!exists) {[m
             prefs.end();[m
[32m+[m[32m            Serial.printf("[ThresholdManager] loadFromNVS FAIL: missing key '%s'\n", key.c_str());[m
             return false;[m
         }[m
     }[m
 [m
     for (auto& pair : thresholds) {[m
[31m-        pair.second = prefs.getFloat(pair.first.c_str(), -1.0f);[m
[31m-        if (pair.second < 0.0f) {[m
[32m+[m[32m        float v = prefs.getFloat(pair.first.c_str(), -1.0f);[m
[32m+[m[32m        Serial.printf("[ThresholdManager] read %s -> %.3f\n", pair.first.c_str(), v);[m
[32m+[m[32m        if (v < 0.0f) {[m
             prefs.end();[m
[32m+[m[32m            Serial.printf("[ThresholdManager] loadFromNVS FAIL: invalid value for '%s' (%.3f)\n", pair.first.c_str(), v);[m
             return false;[m
         }[m
[32m+[m[32m        pair.second = v;[m
     }[m
 [m
     prefs.end();[m
[32m+[m[32m    Serial.println("[ThresholdManager] loadFromNVS OK");[m
     return true;[m
 }[m
 [m
 bool ThresholdManager::saveToNVS() {[m
[32m+[m[32m    Serial.println("[ThresholdManager] saveToNVS() starting");[m
     Preferences prefs;[m
     if (!prefs.begin(NVS_NAMESPACE, false)) {[m
[31m-        Serial.println("ERROR: No se pudo abrir NVS para escritura");[m
[32m+[m[32m        Serial.println("[ThresholdManager] ERROR: No se pudo abrir NVS para escritura");[m
         return false;[m
     }[m
 [m
     for (const auto& pair : thresholds) {[m
         prefs.putFloat(pair.first.c_str(), pair.second);[m
[32m+[m[32m        Serial.printf("[ThresholdManager] wrote %s = %.3f\n", pair.first.c_str(), pair.second);[m
     }[m
 [m
     prefs.end();[m
[32m+[m[32m    Serial.println("[ThresholdManager] saveToNVS OK");[m
     return true;[m
 }[m
[32m+[m
[32m+[m[32mvoid ThresholdManager::debugDump(const char* prefix) const {[m
[32m+[m[32m    Serial.printf("== Thresholds dump: %s ==\n", prefix ? prefix : "");[m
[32m+[m[32m    for (const auto &p : thresholds) {[m
[32m+[m[32m        Serial.printf("  %s = %.3f\n", p.first.c_str(), p.second);[m
[32m+[m[32m    }[m
[32m+[m[32m}[m
[1mdiff --git a/lib/core/ThresholdManager.h b/lib/core/ThresholdManager.h[m
[1mindex c20f494..bf153d9 100644[m
[1m--- a/lib/core/ThresholdManager.h[m
[1m+++ b/lib/core/ThresholdManager.h[m
[36m@@ -20,6 +20,10 @@[m [mstruct Thresholds {[m
 class ThresholdManager {[m
 public:[m
     bool begin();[m
[32m+[m[32m    static ThresholdManager& getInstance() {[m
[32m+[m[32m        static ThresholdManager inst;[m
[32m+[m[32m        return inst;[m
[32m+[m[32m        }[m
 [m
     Thresholds getThresholds() const;[m
     bool setThreshold(const std::string& key, float value);[m
[36m@@ -28,6 +32,8 @@[m [mpublic:[m
 [m
     std::vector<std::string> listKeys() const;[m
 [m
[32m+[m[32m    void debugDump(const char* prefix = "") const;[m
[32m+[m
 private:[m
     std::map<std::string, float> thresholds;[m
 [m
[1mdiff --git a/lib/ui/ConsoleUI.cpp b/lib/ui/ConsoleUI.cpp[m
[1mindex 9d8d14f..23d8dff 100644[m
[1m--- a/lib/ui/ConsoleUI.cpp[m
[1m+++ b/lib/ui/ConsoleUI.cpp[m
[36m@@ -1,5 +1,6 @@[m
 #include "ConsoleUI.h"[m
 #include "CalibrationManager.h" [m
[32m+[m[32m#include <cmath>               // std::isfinite[m
 [m
 void ConsoleUI::begin() {[m
 }[m
[36m@@ -129,6 +130,84 @@[m [mvoid ConsoleUI::interpretarComando(char c) {[m
         }[m
         break;[m
 [m
[32m+[m[32m    case 'i': // Ajustar umbrales INJ ON desde consola (dev only)[m
[32m+[m[32m      if (!devOnly()) break;[m
[32m+[m[32m      this->println(">> Cambiar INJ ON: escribe dos valores min 60 max 90: <INJ_MAP_ON> <INJ_TPS_ON> (ej: 60 70)");[m
[32m+[m[32m      {[m
[32m+[m[32m        Serial.println("[DBG] case 'i' - waiting input");[m
[32m+[m[32m        // Espera línea completa usando inputAvailable() y readLine() (timeout 10s)[m
[32m+[m[32m        unsigned long start = millis();[m
[32m+[m[32m        String line;[m
[32m+[m[32m        while (millis() - start < 15000) { // espera hasta 10s[m
[32m+[m[32m          if (inputAvailable()) {[m
[32m+[m[32m            line = readLine();[m
[32m+[m[32m            line.trim();[m
[32m+[m[32m            if (line.length()) break;[m
[32m+[m[32m          }[m
[32m+[m[32m          delay(5);[m
[32m+[m[32m        }[m
[32m+[m[32m        if (line.length() == 0) {[m
[32m+[m[32m          this->println("⚠️ Tiempo de entrada agotado. Operación cancelada.");[m
[32m+[m[32m          break;[m
[32m+[m[32m        }[m
[32m+[m[32m        // Parsear dos floats[m
[32m+[m[32m        float mapOn = NAN, tpsOn = NAN;[m
[32m+[m[32m        int read = sscanf(line.c_str(), "%f %f", &mapOn, &tpsOn);[m
[32m+[m[32m        if (read < 1) {[m
[32m+[m[32m          this->println("⚠️ Entrada inválida. Formato: <INJ_MAP_ON> <INJ_TPS_ON>");[m
[32m+[m[32m          break;[m
[32m+[m[32m        }[m
[32m+[m[32m        // Si solamente dio uno, pedimos el otro explícitamente[m
[32m+[m[32m        if (read == 1) {[m
[32m+[m[32m          this->printf(">> INJ_MAP_ON = %.1f. Ahora escribe INJ_TPS_ON:\n", mapOn);[m
[32m+[m[32m          start = millis();[m
[32m+[m[32m          String line2;[m
[32m+[m[32m          while (millis() - start < 10000) {[m
[32m+[m[32m            if (inputAvailable()) {[m
[32m+[m[32m              line2 = readLine();[m
[32m+[m[32m              line2.trim();[m
[32m+[m[32m              if (line2.length()) break;[m
[32m+[m[32m            }[m
[32m+[m[32m            delay(5);[m
[32m+[m[32m          }[m
[32m+[m[32m          if (line2.length() == 0) {[m
[32m+[m[32m            this->println("⚠️ Tiempo de entrada agotado. Operación cancelada.");[m
[32m+[m[32m            break;[m
[32m+[m[32m          }[m
[32m+[m[32m          if (sscanf(line2.c_str(), "%f", &tpsOn) != 1) {[m
[32m+[m[32m            this->println("⚠️ Valor INJ_TPS_ON inválido. Operación cancelada.");[m
[32m+[m[32m            break;[m
[32m+[m[32m          }[m
[32m+[m[32m        } else {[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        // Validaciones simples de rango 0..100[m
[32m+[m[32m        // Usamos comprobación NaN-portable[m
[32m+[m[32m        auto isNumber = [](float v) { return v == v; }; // false para NaN[m
[32m+[m[32m        if (!isNumber(mapOn) || !isNumber(tpsOn) || mapOn < 0.0f || mapOn > 100.0f || tpsOn < 0.0f || tpsOn > 100.0f) {[m
[32m+[m[32m          this->println("⚠️ Valores fuera de rango (0..100). Operación cancelada.");[m
[32m+[m[32m          break;[m
[32m+[m[32m        }[m
[32m+[m
[32m+[m[32m        // Actualizar ThresholdManager y persistir[m
[32m+[m[32m        bool okMap = ThresholdManager::getInstance().setThreshold("INJ_MAP_ON", mapOn);[m
[32m+[m
[32m+[m[32m        bool okTps = ThresholdManager::getInstance().setThreshold("INJ_TPS_ON", tpsOn);[m
[32m+[m
[32m+[m[32m        if (okMap && okTps) {[m
[32m+[m[32m          bool saved = ThresholdManager::getInstance().save();[m
[32m+[m[32m          if (saved) {[m
[32m+[m[32m            this->printf(">> Umbrales guardados: INJ_MAP_ON=%.1f, INJ_TPS_ON=%.1f\n", mapOn, tpsOn);[m
[32m+[m[32m          } else {[m
[32m+[m[32m            this->println("⚠️ Error guardando en NVS.");[m
[32m+[m[32m          }[m
[32m+[m[32m        } else {[m
[32m+[m[32m          this->println("⚠️ Error: claves INJ no encontradas en ThresholdManager.");[m
[32m+[m[32m        }[m
[32m+[m[32m      }[m
[32m+[m[32m      break;[m
[32m+[m
[32m+[m
     case 'm':  // Mostrar ayuda[m
       imprimirHelp();[m
       break;[m
[36m@@ -359,7 +438,7 @@[m [mvoid ConsoleUI::imprimirHelp() {[m
   if (developerMode) {[m
     this->println(F("\n🧪 Modo desarrollador activo:"));[m
     this->println(F("  b  → Probar sonido acústico"));[m
[31m-    this->println(F("  u  → (Comando dev pendiente)"));[m
[32m+[m[32m    this->println(F("  i  → Cambiar umbrales INJ ON"));[m
     this->println(F("  x  → Paro manual, volver a IDLE"));[m
     this->println(F("  v  → Visualizar curva TPS-MAP (pendiente desarrollo)"));[m
     this->println(F("  r  → Borrar calibración actual"));[m
[1mdiff --git a/lib/ui/ConsoleUI.h b/lib/ui/ConsoleUI.h[m
[1mindex 64da238..87c6c95 100644[m
[1m--- a/lib/ui/ConsoleUI.h[m
[1m+++ b/lib/ui/ConsoleUI.h[m
[36m@@ -4,6 +4,7 @@[m
 #include "SensorManager.h"[m
 #include "ActuatorManager.h" [m
 #include "Logger.h"[m
[32m+[m[32m#include "ThresholdManager.h"[m
 [m
 class ConsoleUI {[m
 public:[m
[1mdiff --git a/src/main.cpp b/src/main.cpp[m
[1mindex 4ca59a8..852ad1a 100644[m
[1m--- a/src/main.cpp[m
[1m+++ b/src/main.cpp[m
[36m@@ -116,11 +116,13 @@[m [mvoid setup() {[m
   // Cargar calibración y configurar FSM[m
   calib.begin(&sensors);[m
   calibLoaded = calib.loadCalibration();[m
[31m-  thresholdManagerPtr = new ThresholdManager();[m
[31m-  if (!thresholdManagerPtr->begin()) {[m
[31m-    ui->println("❌ Error al iniciar ThresholdManager");[m
[32m+[m
[32m+[m[32m  // Inicializar ThresholdManager usando el singleton[m
[32m+[m[32m  bool t_ok = ThresholdManager::getInstance().begin();[m
[32m+[m[32m  if (!t_ok) {[m
[32m+[m[32m    ui->println("❌ Error al iniciar ThresholdManager (singleton)");[m
   }[m
[31m-  fsm.begin(calibLoaded, &actuators, thresholdManagerPtr, &sensors, &calib);[m
[32m+[m[32m  fsm.begin(calibLoaded, &actuators, &ThresholdManager::getInstance(), &sensors, &calib);[m
   actuators.stopAll();[m
 [m
   ui->println([m
